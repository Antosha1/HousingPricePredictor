image: python:3.7-alpine

test:
  stage: test
  script:
  - pip install -U sphinx
  - sphinx-build -b html ./homeworks/hw1/docs/source ./public
  only:
  - branches
  except:
  - master

pages:
  stage: deploy
  script:
  - pip install -U sphinx
  - sphinx-build -b html ./homeworks/hw1/docs/source ./public
  artifacts:
    paths:
    - public
  only:
  - master
